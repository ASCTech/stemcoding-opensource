= render "form", submission: @submission, course_lab: @course_lab, previous_submissions: @previous_submissions, url: [:student, @course_lab, @submission]

= link_to "Edit in ide", ide_url(query: @signed_message), class: "btn btn-warning"
